version: 1.0
runtime: nodejs14
build:
  commands:
    pre-build:
      - n 14.18.3
    build:      
      - cd frontend && npm install && npm run build
run: 
  command: cd backend && npm install && node server.js
  network:
    port: 3000
    env: APP_PORT